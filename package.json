{
  "name": "linkedin-ads-mcp",
  "version": "1.1.0",
  "description": "MCP server for LinkedIn Ads Marketing API — campaigns, creatives, analytics, demographics, conversions, lead gen",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "linkedin-ads-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "node scripts/stress-test.js && node --test tests/*.test.js"
  },
  "keywords": [
    "mcp",
    "linkedin",
    "ads",
    "marketing",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
